# 图书管理系统毕业设计答辩讲解稿（约8分钟）

各位老师好，我是本次毕业设计《图书管理系统》的答辩学生。  
接下来我会从“整体思路”到“模块设计”进行讲解，重点说明每个模块**做什么**，以及它**为什么必须存在**。

---

首先介绍一下项目目标。  
这个系统主要解决图书借阅场景中的核心问题，包括：图书信息维护、借阅与归还流程、用户权限控制、异常处理和接口统一管理。  
我在设计时采用了分层架构，核心原则是：**职责清晰、低耦合、可扩展、便于维护**。

项目结构分为 annotation、aspect、common、config、controller、dto、entity、exception、repository、security、service 这11个模块。下面我逐个说明。

---

## 1. annotation：自定义注解
这个模块用于定义业务标记，比如操作日志标记、权限标记等。  
它的作用是把“横切关注点”的语义从业务代码里抽离出来。

具体例子：  
我定义了类似 `@OperationLog(module="BOOK", action="ADD")` 的注解，打在“新增图书”接口对应的业务方法上。这样系统就知道这是“图书模块的新增操作”。

为什么它存在？  
如果不使用注解，很多控制逻辑会散落在代码里，导致业务方法可读性下降。通过注解，我可以用声明式方式告诉系统“这个方法需要记录日志”或“这个接口需要某种校验”，提升开发效率，也方便后续统一治理。

## 2. aspect：AOP切面
aspect 模块负责拦截带有特定注解的方法，统一处理日志记录、执行耗时统计、异常增强等横切逻辑。  
它和 annotation 配合使用。

具体例子：  
当管理员执行“删除图书”时，切面会自动记录：操作者ID、接口路径、请求参数、执行时长、是否成功。业务代码里不需要手写这些日志。

为什么它存在？  
因为日志、审计、统一处理这些逻辑如果写进每个业务方法，会造成大量重复代码。AOP 让公共逻辑只写一次，实现“业务归业务，通用归通用”，降低重复和维护成本。

## 3. common：工具类
common 提供通用能力，比如时间处理、分页封装、字符串校验、统一返回结构等。  
它是项目的“基础能力层”。

具体例子：  
在“图书列表查询”接口里，我复用了统一分页工具，前端只传 `pageNum` 和 `pageSize`，后端统一返回 `total`、`records`，不需要每个接口单独封装分页格式。

为什么它存在？  
项目开发过程中会反复出现一些通用处理，如果分散在各处，容易产生多个实现版本，最终行为不一致。放到 common 后可实现复用和规范统一。

## 4. config：配置类
config 模块负责系统级配置，例如跨域配置、序列化规则、拦截器注册、Swagger 或文档配置等。  
它决定系统运行时的基础行为。

具体例子：  
前后端分离开发时，前端端口和后端端口不同，我在配置类里统一放开指定来源的跨域访问，避免每个接口单独处理跨域问题。

为什么它存在？  
配置和业务代码分离，便于不同环境切换，也能避免“硬编码”。后期部署或调优时，主要改配置，不必动核心业务代码，风险更可控。

## 5. controller：接口层
controller 对外提供 REST 接口，接收前端请求，做参数校验和结果返回。  
它是系统的入口层。

具体例子：  
“借阅图书”接口接收到 `userId` 和 `bookId` 后，只做参数合法性校验，然后调用 service 完成借阅流程，最后返回统一结果给前端。

为什么它存在？  
controller 的职责是“接收与转发”，而不是处理复杂业务。把它单独分层后，接口定义更清晰，方便前后端联调，也能保证业务逻辑集中在 service，不会混乱。

## 6. dto：数据传输对象
dto 用于定义接口输入输出的数据结构，比如新增图书请求、借阅请求、分页查询返回等。  
它是接口边界的数据契约。

具体例子：  
在“用户信息返回”里，我用 `UserDTO` 只返回用户名、角色、状态，不返回密码哈希、创建人等数据库内部字段，避免敏感信息暴露。

为什么它存在？  
entity 通常和数据库结构强绑定，直接暴露 entity 容易造成字段泄露或耦合。通过 dto 可以精确控制传输字段，保证安全性和接口稳定性，也便于版本演进。

## 7. entity：数据库实体
entity 模块映射数据库表结构，例如图书、用户、借阅记录等核心对象。  
它用于 ORM 持久化。

具体例子：  
`BookEntity` 对应图书表，包含 `id`、`name`、`author`、`stock`、`availableStock` 等字段；借书时会更新 `availableStock`，还书时再回补。

为什么它存在？  
entity 是数据库和程序对象之间的桥梁。单独建模可以让数据结构清晰稳定，也方便后续数据库字段扩展与迁移。

## 8. exception：异常处理
exception 模块负责自定义业务异常和全局异常处理器。  
比如“库存不足”“图书不存在”“权限不足”等异常，都能被统一捕获并返回规范化错误信息。

具体例子：  
当用户借书时，如果 `availableStock` 为 0，service 会抛出 `BusinessException("该图书库存不足")`，全局异常处理器统一返回错误码和错误信息，前端可直接弹出提示。

为什么它存在？  
如果没有统一异常处理，系统会出现不一致的报错格式，前端很难处理，用户体验也差。该模块保证错误返回可预期，并提升问题定位效率。

## 9. repository：数据访问层
repository 负责数据库读写，包括查询、保存、更新、删除等。  
它是业务层访问数据的标准通道。

具体例子：  
在借阅流程里，service 会调用 `bookRepository.findById(bookId)` 查询图书，再调用 `borrowRecordRepository.save(record)` 落库借阅记录，流程清晰且职责分离。

为什么它存在？  
把数据访问逻辑从业务层剥离，可以避免 SQL 或持久化细节污染业务代码。后续若更换数据源或优化查询，只需在 repository 层调整，影响面更小。

## 10. security：安全认证
security 模块实现登录认证、权限校验、密码加密、访问控制等安全机制。  
例如区分管理员与普通用户的接口权限。

具体例子：  
“新增图书、删除图书”接口只允许管理员访问，普通用户即使知道接口地址也会被权限拦截，返回“无权限访问”的统一结果。

为什么它存在？  
图书管理系统虽然是教学项目，但依然涉及用户身份和关键操作。安全模块独立后，认证授权逻辑可统一维护，避免在每个接口手写权限判断，减少安全漏洞风险。

## 11. service：业务层
service 是系统核心，负责组织业务流程，比如“借书时检查库存、检查用户状态、生成借阅记录、更新图书可借数量”等。  
它连接 controller 和 repository。

具体例子：  
以“还书”为例，service 会先校验借阅记录是否存在、是否已归还；校验通过后更新借阅状态为已归还，同时把图书可借库存加1，最后写入归还时间。

为什么它存在？  
业务规则必须集中管理，不能分散在接口层或数据库层。service 的存在保证业务逻辑可复用、可测试、可维护，也是系统可持续迭代的关键。

---

## 架构总结
最后总结一下本项目的架构设计。  
我采用的是典型的分层架构并结合 AOP 与安全模块，整体调用链是：

**Controller -> Service -> Repository -> Database**  
同时由 **DTO** 作为接口边界、**Entity** 作为持久化模型、**Exception** 统一错误处理、**Security** 保障访问安全、**Annotation + Aspect** 处理横切逻辑、**Common + Config** 提供基础支撑。

这种设计的价值主要有三点：  
第一，职责分离明确，便于团队协作和后期维护。  
第二，模块之间低耦合，功能扩展时改动范围可控。  
第三，工程化规范完整，具备从课程项目向实际业务系统演进的基础。

我的讲解完毕，感谢各位老师聆听，欢迎指正。
