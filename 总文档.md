# 智慧图书馆管理系统 - 总文档

## 1. 项目概述

本项目为毕业设计作品，名称为 **智慧图书馆管理系统（Smart Library Management System）**。  
采用前后端分离架构，支持图书管理、用户管理、借阅归还、预约、评论、公告、罚款与统计分析等核心业务场景。

- 前端：`admin.html`（管理端）、`user.html`（用户端）
- 后端：`library-management`（Spring Boot）
- 数据库：MySQL 8.0

## 2. 技术栈

### 2.1 后端

- Java 17
- Spring Boot 3.2.1
- Spring Security + JWT
- Spring Data JPA + Hibernate
- MySQL 8.0
- Maven
- SpringDoc OpenAPI（Swagger）
- JUnit 5 + Mockito

### 2.2 前端

- Vue 3（CDN 方式）
- Vue Router 4
- Pinia
- Element Plus
- Tailwind CSS
- ECharts

## 3. 项目结构

```text
d:\桌面\1
├─ admin.html                     # 管理端
├─ user.html                      # 用户端
├─ js
│  ├─ common.js                   # 公共JS模块（HTTP封装/工具函数）
│  └─ README.md
├─ library-management             # 后端Spring Boot项目
│  ├─ pom.xml
│  └─ src
│     ├─ main
│     │  ├─ java/com/library
│     │  │  ├─ controller         # 控制层
│     │  │  ├─ service            # 业务层
│     │  │  ├─ repository         # 数据访问层
│     │  │  ├─ entity             # 实体层
│     │  │  ├─ dto                # 请求/响应对象
│     │  │  ├─ security           # 认证授权
│     │  │  ├─ config             # 配置类
│     │  │  ├─ common             # 统一返回等
│     │  │  └─ exception          # 全局异常处理
│     │  └─ resources
│     │     ├─ application.yml
│     │     ├─ application-prod.yml
│     │     └─ schema.sql
│     └─ test/java/com/library/service
└─ 项目答辩报告.md
```

## 4. 运行环境要求

- JDK 17+
- Maven 3.8+
- MySQL 8.0+
- 现代浏览器（Chrome/Edge）

## 5. 数据库准备

1. 创建数据库（示例）：

```sql
CREATE DATABASE library_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. 执行初始化脚本：

- 路径：`library-management/src/main/resources/schema.sql`

3. 检查后端配置：

- 开发环境：`library-management/src/main/resources/application.yml`
- 生产环境：`library-management/src/main/resources/application-prod.yml`

## 6. 启动方式

### 6.1 启动后端

方式一：

```bash
cd library-management
mvn spring-boot:run
```

方式二：

```bash
mvn spring-boot:run -f library-management/pom.xml
```

启动后默认地址：

- API：`http://localhost:8080/api`
- Swagger：`http://localhost:8080/swagger-ui/index.html`

### 6.2 访问前端

- 管理端：直接浏览器打开 `admin.html`
- 用户端：直接浏览器打开 `user.html`

## 7. 默认账号

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员 | admin | admin123 |
| 馆员 | librarian | admin123 |
| 普通用户 | kerry | admin123 |

## 8. 核心功能清单

- 用户认证与授权（JWT、角色权限）
- 图书管理（新增、编辑、删除、查询、封面上传）
- 借阅管理（借书、还书、续借、逾期处理）
- 分类管理（树形分类）
- 用户管理（启用/禁用、审核）
- 预约管理（排队、通知）
- 评论管理（用户评论、后台审核/管理）
- 收藏功能
- 公告管理
- 罚款管理（规则、记录、免除、支付标记）
- 通知中心（站内消息）
- 统计分析（仪表盘、趋势、热门图书、活跃用户）
- Excel 导出
- 操作日志（AOP）

## 9. 主要接口模块（后端）

- `/api/auth`：登录、注册、当前用户
- `/api/books`：图书管理
- `/api/borrows`：借阅管理
- `/api/categories`：分类管理
- `/api/users`：用户管理与审核
- `/api/reviews`：评论管理
- `/api/announcements`：公告管理
- `/api/fines`：罚款管理
- `/api/notifications`：通知管理
- `/api/statistics`：统计分析
- `/api/files`：文件上传
- `/api/logs`：操作日志

## 10. 测试说明

当前已包含服务层单元测试（`src/test/java/com/library/service`）：

- `BorrowServiceTest`
- `ReservationServiceTest`
- `UserServiceTest`

运行测试：

```bash
mvn test -f library-management/pom.xml
```

## 11. 常见问题

### 11.1 后端启动失败

- 检查 MySQL 是否启动
- 检查数据库连接信息是否正确
- 检查 JDK 版本是否为 17+

### 11.2 前端请求失败（Failed to fetch）

- 确认后端是否运行在 `8080`
- 确认浏览器控制台是否有跨域或认证错误
- 确认登录后 token 已写入 localStorage

### 11.3 Swagger 打不开

- 确认后端已成功启动
- 访问 `http://localhost:8080/swagger-ui/index.html`

## 12. 交付建议（答辩版）

建议同时提交以下材料：

- 本文档（总文档）
- 毕设论文
- 项目答辩报告
- 测试报告
- 用户手册
- 部署手册
- 演示PPT与演示脚本

