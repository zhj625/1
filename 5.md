全日制本科生毕业设计

智慧图书馆管理系统的设计与实现

学    院：计算机与信息科学学院
专业年级：计算机科学与技术专业22级
学生姓名：曾海军   学  号：2022051614021
指导教师：范永胜

2026年  4月  12日

摘 要

十年前的图书馆，一个管理员守着几排书架，谁借谁还登记一下，够用了。但是现在馆藏动不动几万册，每天几百人进出，还是那套手动登记加 Excel 的老办法，完全跟不上节奏。高峰期前台排队排到门外头，后台的数据更是一团糟。逾期的书追不回来，罚款算不清楚，到了年底领导要看报表了才发现数据对不上。权限管理基本等于没有，谁都能改数据。这些问题攒了好几年，补丁叠补丁也没法根治。

这个项目就是冲着这些痛点去的。技术方案我前前后后对比了好几轮才定下来：后端最终选了 Spring Boot 3.2.1，搭配 Spring Security 做权限过滤，JWT 来做登录凭证的无状态传递。数据库是 MySQL 8.0，用 Spring Data JPA 做对象关系映射。有一个小地方花了不少功夫，就是在借阅接口上加了乐观锁，怕出现好几个人同时抢最后一本书把库存减成负数的情况。前端没用 JSP 那种老的渲染方式，选了 Vue 3 配 Pinia 管状态，界面组件直接用 Element Plus。

用的人不同，看到的界面也不一样。管理员那边能建账号、调权限、看全局数据；馆员登进去就是借书还书续借这些日常操作的界面；普通读者只能在自己的页面上搜书、借书、预约——三套入口各干各的事，互不干扰。功能这块，借阅归还续借跑通了完整流程，分类目录按树形结构做的可以一直往下建子分类，罚款按逾期天数自动累加，预约的书到馆了系统会推通知。另外前端拿 ECharts 做了一个图表看板，馆员打开就能看见这个月的借还量曲线和各类书的占比饼图。

测试阶段重点盯了两个地方，一个是并发借阅会不会把库存打穿，一个是 JWT 过期或者权限不对的时候系统能不能正确拦截。跑了好几轮，都没出问题。实际操作起来的手感比那些老旧的桌面端软件好很多，页面响应很快，流程也比较顺。整套系统拿去给中小型图书馆或者高校的系级资料室日常用，完成度是够的。

关键词：智慧图书馆；前后端分离；Spring Boot；Vue 3；JWT；乐观锁

1 绪论

1.1 选题背景与意义

数字化这件事推了这么多年，银行转账在手机上点两下就完成了，医院挂号也不用去窗口排队了，就连大学教务系统都已经把选课、查成绩、评教全搬到了线上。图书馆呢？明显落后了一截。书一年比一年买得多，来借书的人也没见少，偏偏管理手段多年不变。还有不少地方在用 Excel 登记进出库信息，纯靠人工维护。

往下面看就知道有多糟糕。不少中小型公共图书馆和高校的系级资料室，管理系统还是好多年前花钱买的 C/S 架构桌面程序，装在一台固定的电脑上才能用。更差一些的，连软件都没装，管理员就拿本子记。书少的时候问题不大，一旦馆藏量涨上去，什么书在谁手上、逾期了几天、罚款交没交，往本子上翻来翻去半个小时都捋不明白。

还有一个技术上的硬伤是并发处理。借书看着就是点一下按钮的事，但要是同一时间好几个人都去借同一本书，没有并发保护的老系统压根不会拦截——每一笔操作都正常执行，库存从 1 减到 0 再减到负数，系统不弹任何警告。直到管理员下次盘点库存，对着表上 -2 的数字发懵，才知道出了问题。类似的事情在用老系统的图书馆不算罕见。

安全方面的隐患也不小。早期开发的那些管理程序很多没有做参数过滤，请求内容直接拼进SQL语句里，基本的注入攻击都防不住。至于数据统计和报表输出，大部分老系统压根没有这个功能——要出一份年度报告，只能把数据手工导到 Excel 里自己整理，效率极低。

所以开发一套现代化的图书管理系统，不是在追求什么技术噱头，是这个缺口确实存在。大量基层图书管理单位需要一个部署简单、使用成本低、功能够用的替代方案，而市面上的商业产品要么太贵要么太重，并不适合他们。

1.2 国内外研究现状与发展趋势

欧美高校的图书馆信息化起步比国内早了二十多年，目前的系统成熟度也确实领先。现阶段国际上主流使用的是被称为 LSP（下一代图书馆服务平台）的一类产品，代表性的有 Ex Libris 旗下的 Alma 和 OCLC 推出的 WMS。这些平台普遍已经迁移到了云端部署，后台按照微服务架构拆分，纸质馆藏和电子资源的管理打通在同一个系统里，不再分开处理。整体来看技术水平和系统集成度都比较高。

国内的情况分化非常严重。985、211层级的高校图书馆普遍已经采购了大型商业管理平台，系统功能比较完善，可以做到图书实时定位、读者行为分析这些精细化管理。但到了普通本科院校的系级资料室，或者区县一级的公共图书室这个层面，情况就是另一个样子了。相当一部分机构还在使用技术栈严重老化的系统，有的基于 Struts 或者纯 JSP 开发，前端页面和后端逻辑耦合在同一份代码里，想改个界面都可能不小心破坏原有的业务逻辑，日常维护和后续扩展的代价都很高。经费紧张是这些单位普遍面临的约束，"不是不想换，是换不起"是很常见的说法。

好在最近几年开源技术生态发展得比较快。Spring Boot 把 Java 后端开发的入门门槛拉低了不少，前端框架 Vue 和 React 也让界面开发不再那么依赖专业前端工程师。前后端分离加 REST 接口通信的架构模式已经是行业里的标准做法了。这意味着用现成的开源组件组合起来，完全有条件针对中小型图书馆的场景做出一套成本低、部署简单、够用好用的管理系统。

1.3 研究目的与工程意义

研究目的：

这篇论文要做的事情很清楚：用 Spring Boot 3 搭后端、Vue 3 搭前端，从零开始造一个 B/S 架构的智慧图书管理系统。工程目标说具体一点，就是要把图书从入库到报废的整个生命周期打通管起来——注册审核怎么走、借阅规则怎么判、实物流转怎么追踪、运营数据怎么看板化呈现，每一环都得有落地方案。

研究意义：

理论和工程两头都有价值。先说技术整合方面：AOP 切面用来做日志监控，不侵入业务代码本身就是一种架构上的洁癖；JWT 的防篡改特性解决了分布式场景下的跨域鉴权难题；乐观锁在关系型数据库上保障事务一致性。把这些工业界的标准做法集中放到一个项目里，等于是给"现代 Web 系统开发该怎么干"做了一次完整的技术示范。

2.1 后端核心架构技术

2.1.1 Spring Boot 3 框架

Spring 体系的根基建立在两个核心概念上：控制反转（IoC）和依赖注入（DI）。翻译成大白话就是——你写的那些 Service、Repository 对象，从创建到销毁全交给 Spring 容器打理，开发者不用自己 new，也不用操心对象之间怎么找到彼此。这样做最直接的好处是解耦：组件之间的依赖关系配置化了，想换实现就换，单元测试也方便 mock。

说回 AOP，面向切面编程。这个概念解释起来有点绕，但它干的事情说出来就很直白：你有一段通用逻辑，比如每个接口被调用的时候都需要记一条操作日志，或者每次数据库写操作前后都要管理事务。按照正常想法你得在每个方法里都写一遍这些代码，几十个接口就要复制粘贴几十遍。AOP 的做法是把这段逻辑单独抽出来写成一个"切面"，框架在运行阶段自动帮你嵌进去。具体怎么嵌？如果目标类实现了接口就走 JDK 动态代理，没有接口的类就用 CGLIB 在字节码层面生成子类来代理。我调试乐观锁逻辑那阵子被 AOP 的代理层坑过一次，断点明明打在我写的方法上，实际执行的时候中间多了好几层代理类的调用栈，追了半天才定位到问题。

Spring Boot 到了 3.2 这一代变化挺大的。最明显的一点：以前那些写了无数行的 XML 配置文件，全废了——不支持了。另外 JDK 版本最低要求直接拉到 17，连 11 都不行。真正省事的地方在自动装配这块，框架启动时通过 @EnableAutoConfiguration 去 classpath 里翻，看你引了什么 starter 依赖，就自动把对应的组件给你装配好。比方说 pom 里加了 web starter，嵌入式 Tomcat 自动拉起来；加了 JPA starter，数据源连接池自动配好。写过以前那种手动配 DataSource 的人，切过来之后会有一种"白干了那么多年"的恍惚感。

Vue 3 跟上一代相比，最大的理论跃迁在于 Composition API。以前 Vue 2 的 Options API 把 data、methods、computed 切成固定板块，一个功能的逻辑散落在三四个地方，项目一大就难受；Composition API 反过来，让你按逻辑主线而不是语法分类来组织代码——同一件事的响应式数据和操作函数写在一个 setup 块里，读起来顺畅得多。

再说前端，Vue 3 在响应式这块动了大手术。Vue 2 那会儿用 Object.defineProperty 来监听数据变化，这个 API 本身就有缺陷——你往一个对象上动态加个新属性，它检测不到；直接用下标改数组某一项的值，它也不知道。写代码的时候经常碰到数据明明改了，页面愣是不刷新的情况，逼得人去查文档才发现要用 Vue.$set 方法。Vue 3 换成了 ES6 的 Proxy 做底层拦截，这种问题彻底消失了——不管你怎么改对象，嵌套多深它都能感知到。就这一个改动，开发体验就提升了一大截。

状态管理从 Vuex 换到 Pinia，我个人觉得是个正确的决定。Vuex 里面搞一个状态修改要走 commit mutation 这套固定流程，写多了烦得很，而且 TypeScript 的类型推断在 Vuex 里一直不太好使。Pinia 把 mutation 这个概念直接砍了，store 里面 state、getter、action 三件套就够了，代码量少了将近一半。加上 Element Plus 组件库——它家的 Table 组件、Form 校验、Tree 树形控件做得确实不错——前端界面的呈现效果直接对齐了主流企业后台的水准。

超级管理员控制层（Admin / Superuser View）：

这个角色说白了就是"大管家"，平时不管借书还书这些具体的事，但手里攥着最大的权力。比如给某个馆员提权或降级——就是点两下的事。遇到那种书借了三个月不还、罚款也不交、打电话不接的"老赖"读者，这一层可以直接把账号冻结。什么时候解冻、要不要解冻，全看管理员的判断。

另外有一项权限是独占的——图书分类目录的最顶层结构维护。打个比方，中图分类法里面"自然科学"底下挂着"力学"，"力学"底下又挂着"粒子物理"，这整棵树从根节点到任意层级的创建和调整，只有超管有权操作。还有那个统计数据大屏——借还趋势曲线、各类别藏书占比、月活跃用户数之类的汇总看板——也是超管专属入口，其他角色看不到。

3.2 核心业务需求分析

不同角色在系统里的操作逻辑差异很大，但有几条底线是必须守住的，不管谁来操作都不能突破：

安全鉴权方面：

凡是不对外公开的接口，请求进来必须带合法的 JWT Token，否则直接返回 401。这是最基本的一道门。为了防止有人写脚本暴力试密码，还设了登录失败的计数器——连续错 5 次，账号锁定 15 分钟。这种措施技术上不复杂，但如果不做，等于把大门敞开了。

借阅流程要过的几道关卡：

很多人以为借书就是"扫码，库存减一，完事"。实际上系统后台在你点那个借阅按钮之后做了一连串判断。先查你手上还着几本书，我们限额设的 5 本，够了就直接拒绝，不会往下走了。然后看你是不是有未处理的逾期记录或者罚款没交，有的话这一步也过不去，必须先解决历史遗留问题。这两步过了才轮到技术上最棘手的一关。

最棘手的地方在哪呢？并发。一本很火的教材库存只有最后 1 本的时候，如果恰好三四个人几乎同一秒点了借阅，正常不加任何保护的话，他们有可能各自读到库存为 1，各自减 1，最后库存变成 -3。真实发生过这种事，前面说了。我处理这个的办法是给 books 表加了一个 version 字段，每次 UPDATE 的时候 WHERE 条件里带上 version = 当前版本号，改的同时 version 自增。如果两个请求撞上了，先执行完的那个把 version 改了，后面那个 UPDATE 影响行数为 0，直接回滚。这就是乐观锁的思路，写起来代码不多、但不加的话后果很严重。

分类管理的表结构设计：

图书分类这个东西看着简单，做起来有点讲究。我们学校图书馆的分类体系是按照中图法来的，大类下面套小类，小类下面还能再分。为了支持这种不确定有多少层的树形结构，数据库表里的设计是让每条分类记录有一个 parent_id 字段指向自己的上一级。顶级分类的 parent_id 就设为 null。前端展示的时候，用户点开某个分类节点，页面再去请求它的子分类列表，懒加载的方式，不会一次性把整棵树全拉下来。这种自引用的设计我在网上查了好几篇博客，写法大同小异，关键是 JPA 实体类里 @ManyToOne 的映射不能写错，不然查出来的树形结构会乱套。

报表那块：

管理人员经常需要知道这个月借出去多少本、还回来多少本，哪一类书最受欢迎。我在前端用 ECharts 画了折线图和饼图，数据从后端的统计接口拿。另外还做了 Excel 导出功能，因为有些馆长要把季度数据打印出来存档或者交给上级领导看，光看屏幕上的图表不行。说实话 Excel 导出这个功能代码量不大，但用 Apache POI 处理中文表头编码的时候踩了几个坑，来来回回试了几次。

4 系统架构与数据库设计

4.1 系统总体架构设计

系统运行依赖一条完整的前后端分离链路。浏览器端发出请求，先过 Spring Security 的过滤器链做身份校验和权限判定，通过后路由到对应的 Controller，Controller 调 Service 层处理业务逻辑，Service 再通过 Repository 操作数据库。返回的数据原路回去，JSON 格式在前后端之间来回流转，各管各的，互不侵入。

图4-1 系统全局微观请求流转架构图

4.2 数据库概念模型与 ER 图

数据库模型设计得好不好，直接决定后期会不会频繁遇到慢查询或者脏读的坑。系统涉及的业务表一共11张，围绕图书流通形成一个完整闭环。

图4-2 智慧图书库主要业务实体关系概念模型映射简图

表结构定义摘录

拿最核心的图书资源表 (books) 来做示例说明：

辅助表结构定义释义

核心书库之外，人员档案和流通记录的辅助表同样遵循三范式（3NF）设计：

表4-2 系统用户凭证主表 (users)
表4-3 借出流水与生命周期记录表 (borrow_records)
表4-4 超时财务罚金记录表 (fines)

系统在鉴权这件事上做了一个干脆的决定：彻底抛弃 Servlet Session，全面用 JWT。具体实现上，在 Spring Security 过滤器链的最前端插入一个自定义的 Filter，每个请求进来先被它截住做 Token 解析和校验。

Token 签发与防伪的实现细节：

JWT 的好处在于自包含——Token 本身就携带了用户身份和权限信息，服务端不用存 Session，天然适配无状态架构。用户通过 /api/login 提交凭证、AuthenticationManager 校验通过后，签发流程如下：

```java
public String generateToken(UserDetails userDetails) {
    Map<String, Object> claims = new HashMap<>();
    // 把用户的 RBAC 角色列表塞进 Payload
    claims.put("roles", userDetails.getAuthorities());

    return Jwts.builder()
            .setClaims(claims) // 载入用户信息声明
            .setSubject(userDetails.getUsername()) // 标记Token归属的用户主体
            .setIssuedAt(new Date(System.currentTimeMillis())) // 签发时间戳
            // 过期时间设定为10小时后
            .setExpiration(new Date(System.currentTimeMillis() + 1000 * 60 * 60 * 10))
            // 用 HMAC-SHA256 算法 + 密钥生成签名
            .signWith(SignatureAlgorithm.HS256, jwtConfigSecret)
            .compact();
}
```

最终生成的 Token 字符串由三部分拼接而成：Base64Url 编码的 Header、同样编码的 Payload、以及根据前两者加密钥运算出来的 Signature。后续每次请求受保护接口时（比如 /api/books/list），自定义的 OncePerRequestFilter 子类会从请求头的 Authorization: Bearer <Token> 字段里把 Token 取出来，验签、检查过期时间、提取用户信息——全套流程在一个 Filter 里走完。

5.4 基于 Specification 的多条件查询实现

Specification 这种写法我刚接触的时候觉得有点多此一举，后来才体会到好处。前端的搜索栏可能只填了书名，也可能书名和分类都选了，甚至什么都没填直接点搜索。如果用固定的 SQL 来写，每种情况都要写一条，排列组合下来很多。用 Specification 就是在代码里判断哪个参数有值就拼那个查询条件进去，没传的参数对应的那段 WHERE 子句压根不会出现在最终的 SQL 里。后面如果要加按作者搜索或者按出版年份筛选，只要再加一段 if 判断就行，老的代码一行都不用动。

```java
// 按需组装查询条件
Specification<Book> spec = (root, query, cb) -> {
    List<Predicate> predicates = new ArrayList<>();

    // 有关键词就加模糊匹配
    if (StringUtils.hasText(keyword)) {
        predicates.add(cb.like(root.get("title"), "%" + keyword + "%"));
    }
    // 选了分类就加等值匹配
    if (categoryId != null) {
        predicates.add(cb.equal(root.get("category").get("id"), categoryId));
    }
    return cb.and(predicates.toArray(new Predicate[0]));
};

// 带上分页参数丢给 Repository 执行
Page<Book> booksPage = bookRepository.findAll(spec, pageRequest);
```

6 系统测试与分析

开发到后期我花了差不多两周专门做测试。坦白讲，写代码的时候总觉得逻辑没问题，但一到测试阶段就会发现一些预料之外的情况——有些甚至是自己当初想当然以为不会出错的地方。

6.1 核心用例测试结果分析

我用的是黑盒测试的方式，就是不看代码内部逻辑，只从用户操作的角度去验证结果。测的场景挑了几个最关键的：一个已经借了 5 本书的用户再去借第 6 本，看系统返回的是不是 400 错误而不是静默通过。把一个已经过期两个小时的 JWT Token 拿去请求查询接口，看返回的是 401 还是正常数据。然后就是并发那个测试——我开了两个浏览器窗口登着不同账号，对着同一本库存只剩 1 的书几乎同时点借阅，看是不是只有一个能成功。这几个测试如果有任何一个没通过，都属于比较致命的 bug。最终结果都符合预期，这才放心提交。

6.2 系统并发性能测试分析

功能对了不等于扛得住压力。一百个人同时访问和一个人访问是完全不同的场景。性能测试用的是 Apache JMeter，针对系统里被访问得最频繁的两个接口——多条件联合检索和分页查询——做持续并发压测。

测试配置是百线程级别的持续不间断请求。跑下来的数据显示，在长时间高并发负载下系统的吞吐量稳定维持在每秒600笔以上的请求处理能力。后台资源监控那边，CPU 占用和 JVM 内存消耗始终在安全水位以下，整个测试周期内没有触发过 OutOfMemoryError，也没有发现因为乐观锁重试导致的死循环或者服务挂起之类的异常。

这组数据足以说明问题。抛开老旧的模板引擎混合方案，换成 Spring Data 配合前后端分离的响应式架构后，系统在中等规模的并发压力下表现出了足够的弹性和稳定性。拿去应对高校图书室日常运营的流量峰值，不会有大问题。

7 总结与展望

7.1 工作总结

这篇论文从需求分析到架构设计，再到编码实现和测试验证，完整走了一遍基于 Spring Boot 3 与 Vue 3 的图书馆管理系统开发流程。针对传统模式在交互效率、并发安全、权限管控上的具体短板，每一处都给出了对应的技术解决方案并落实到了代码层面。

后端实现了多级分类管理、借阅流程控制、并发防超卖、JWT 无状态认证等核心模块。前端放弃了模板渲染，做成了组件化的响应式界面。最终交付的系统在功能完整性和安全防护能力两方面都经过了单元测试和集成测试的检验，达到了工程级管理系统的设计要求。

7.2 后续展望与不足

系统目前的功能闭环是完整的，但如果放到更大规模的真实场景里去跑，有两个方向需要做后续迭代：

缓存层的补充：

现在的检索和鉴权操作每次都直接查数据库。日常使用没什么感觉，但遇到集中选书之类的高峰时段，数据库的 I/O 压力会成为瓶颈。后续版本应该引入 Redis 做热点数据缓存，把高频查询的结果先存在内存里，既能提高响应速度也能给数据库减压——技术上叫削峰，原理很直观。

推荐功能我目前没来得及做，但思路有了。用户的借阅记录攒多了之后本身就是有价值的数据。谁借了什么书这个信息，喂给协同过滤算法跑一跑就能得出"借了 A 书的人往往也会借 B 和 C"这种规律。以后如果要做个"你可能感兴趣的图书"的推荐栏，后端加一个定时任务去跑模型、前端加一个展示位就行了。数据已经在了，缺的是算法实现和界面，留给后续迭代。

附录

附录A：系统本地部署与运行手册

要跑起来需要两样东西：JDK 17 和 MySQL 8.0。别的不需要。

后端启动很简单：

```bash
# 在后端工程目录里执行
mvn spring-boot:run
```

看到控制台打印出 Tomcat 启动成功、端口 8080 就绑定了的日志，就说明后端已经就绪。第一次启动的时候 Hibernate 会自动根据实体类建表——不用手动执行任何 SQL 脚本。

前端怎么访问？更简单。

这个项目的前端没有走 npm install 再 npm run build 那条路，所有的 Vue、Element Plus 依赖都是从公共 CDN 拉取的。所以启动方式就是用浏览器（推荐 Chrome）直接打开项目根目录下的 HTML 文件——user.html 是读者前台，admin.html 是管理后台。打开之后页面会自动去请求 localhost:8080 的后端接口，只要后端已经跑起来了，前后端就自动对接上了。

附录B：核心依赖工程配置说明 (pom.xml)

Maven 项目的依赖配置，按功能大致分成四块：

支撑 REST 接口通信的 spring-boot-starter-web 是基础骨架，全局版本由 spring-boot-starter-parent 统一锁在 3.2.1——这个 Parent POM 的作用就是帮你管好版本号，免得 A 依赖要求某个库用 2.x，B 依赖又要求同一个库用 3.x，最后打包时报一堆冲突。

数据层的 spring-boot-starter-data-jpa 做 ORM 映射，mysql-connector-j 提供 MySQL 的 JDBC 连接驱动。

安全相关的 spring-boot-starter-security 负责过滤器链级别的请求拦截和权限判定，jjwt-api 加 jjwt-impl 这对组合包负责 JWT 令牌的生成、解析和签名校验。
