# 系统各模块详细作用说明

本文档根据当前项目代码实际结构整理，逐个说明控制器、业务实现类、实体类、Repository 与单元测试的具体作用，适合直接用于论文说明、项目文档或答辩讲解。

## 一、统计口径说明

根据当前代码目录统计，项目中实际包含：

- 控制器：15 个
- 业务实现类：12 个
- 实体类：13 个
- Repository：12 个
- 单元测试：3 组

其中，你之前写的“14 个控制器”与当前代码略有差异。当前 `controller` 目录中实际存在 15 个控制器，包含 `CoverController` 与 `FileController` 这两个文件类接口控制器。如果你在论文中只想按 14 个来写，通常需要明确说明是否将其中某一个工具型控制器不纳入业务控制器统计。

---

## 二、15 个控制器的详细作用

控制器位于系统接口层，主要负责接收前端请求、调用业务层处理逻辑，并将结果返回给前端。

### 1. `AuthController`

作用：负责系统认证与当前用户相关的基础接口，是用户登录和身份获取的入口。

主要职责：

- 处理用户登录请求
- 处理用户注册请求
- 获取当前登录用户信息
- 处理用户修改密码操作

说明：该控制器是身份认证模块的核心入口，直接关系到系统的登录鉴权与账号安全。

### 2. `AnnouncementController`

作用：负责公告模块接口，对系统公告进行新增、修改、删除、发布、撤回和查询。

主要职责：

- 新增公告
- 修改公告
- 删除公告
- 根据编号查询公告
- 分页查询公告列表
- 查询已发布公告
- 查询最新公告
- 发布公告与取消发布
- 切换公告置顶状态

说明：该控制器支撑系统消息公示、通知展示与后台公告管理功能。

### 3. `BookController`

作用：负责图书管理模块接口，是图书信息维护和查询的主要入口。

主要职责：

- 新增图书
- 修改图书信息
- 删除图书
- 查询单本图书详情
- 分页查询图书列表
- 查询新到图书
- 查询热门图书

说明：该控制器承担图书基础数据的管理，并为前端首页推荐和图书列表页面提供支持。

### 4. `BorrowController`

作用：负责借阅业务接口，是借书、还书、续借、缴纳借阅罚金等流程的接口入口。

主要职责：

- 发起借书
- 办理还书
- 办理续借
- 支付借阅记录关联罚金
- 查询借阅记录详情
- 分页查询借阅记录
- 查询当前用户个人借阅记录
- 导出借阅记录 Excel

说明：该控制器直接连接图书流转业务，是图书馆管理系统中最核心的业务控制器之一。

### 5. `CategoryController`

作用：负责图书分类模块接口，用于管理图书分类体系。

主要职责：

- 新增分类
- 修改分类
- 删除分类
- 查询分类详情
- 查询全部分类
- 查询分类树结构

说明：该控制器保证图书分类结构清晰，方便图书检索、筛选和归类管理。

### 6. `CoverController`

作用：负责封面生成接口，用于生成图书封面相关的 SVG 图像内容。

主要职责：

- 根据请求参数生成封面图像

说明：这是一个偏工具型的控制器，主要服务于图书封面展示或默认封面生成，不承担复杂业务逻辑。

### 7. `FavoriteController`

作用：负责收藏模块接口，用于读者收藏图书与查看收藏状态。

主要职责：

- 添加收藏
- 取消收藏
- 分页查询我的收藏
- 检查某本书是否已被当前用户收藏
- 查询某本书的收藏数量

说明：该控制器增强了读者个性化使用体验，也可为图书热度分析提供辅助数据。

### 8. `FileController`

作用：负责文件上传接口，主要用于图书封面或其他资源上传。

主要职责：

- 上传文件
- 返回文件访问路径或结果信息

说明：该控制器为系统提供文件资源接入能力，常用于图书封面上传、素材管理等场景。

### 9. `FineController`

作用：负责罚款规则与罚款记录模块接口，是逾期罚款业务的管理入口。

主要职责：

- 查询当前罚款规则
- 更新罚款规则
- 分页查询罚款记录
- 查询罚款记录详情
- 执行罚款减免
- 查询罚款统计信息
- 查询当前用户个人罚款记录
- 查询当前用户未支付罚款总额
- 支付罚款

说明：该控制器将罚款规则配置与罚款记录处理统一管理，方便管理员与用户分别操作。

### 10. `NotificationController`

作用：负责通知消息模块接口，用于通知列表读取与已读状态维护。

主要职责：

- 分页查询当前用户通知
- 查询未读通知数量
- 将单条通知标记为已读
- 将全部通知标记为已读

说明：该控制器保证系统的借阅提醒、逾期通知、预约通知等消息能够被有效接收和管理。

### 11. `OperationLogController`

作用：负责系统操作日志查询接口，用于审计和后台运维管理。

主要职责：

- 分页查询操作日志
- 查询单条操作日志详情

说明：该控制器不直接处理业务功能，而是服务于系统管理、安全审计和问题追踪。

### 12. `ReservationController`

作用：负责预约业务接口，是图书预约、取消预约和排队查询的入口。

主要职责：

- 预约图书
- 取消预约
- 分页查询我的预约记录
- 查询我的有效预约
- 检查某本书是否存在当前用户有效预约
- 查询某本书的预约排队长度

说明：该控制器用于解决图书库存不足时的排队预约场景，是借阅业务的重要补充。

### 13. `ReviewController`

作用：负责图书评论模块接口，用于读者评论发布、展示与后台审核管理。

主要职责：

- 新增评论
- 查询某本书的评论列表
- 查询当前用户自己的评论
- 查询最新评论
- 删除评论
- 对评论点赞
- 管理员分页查看全部评论
- 管理员删除评论
- 管理员修改评论状态

说明：该控制器兼顾前台互动与后台审核，支撑图书评价与用户交流功能。

### 14. `StatisticsController`

作用：负责统计分析模块接口，用于生成仪表盘和高级统计数据。

主要职责：

- 查询基础统计数据
- 查询首页仪表盘数据
- 查询高级统计数据

说明：该控制器主要面向后台管理端，为管理员提供系统运行情况和业务数据分析能力。

### 15. `UserController`

作用：负责用户管理模块接口，用于用户查询、状态管理、审核与删除。

主要职责：

- 分页查询用户列表
- 查询用户详情
- 更新用户状态
- 删除用户
- 查询待审核用户
- 审核通过用户
- 驳回用户

说明：该控制器主要服务于后台管理员，对用户账号状态和审核流程进行统一管理。

---

## 三、12 个业务实现类的详细作用

业务实现类位于系统业务层，负责承接控制器调用，真正实现系统业务规则。

### 1. `AnnouncementServiceImpl`

作用：实现公告管理业务逻辑。

主要负责：

- 创建公告并记录发布人信息
- 修改公告内容、类型、状态等数据
- 删除公告
- 根据条件分页查询公告
- 查询已发布公告与最新公告
- 执行公告发布、撤回和置顶切换

业务意义：保证公告从草稿到发布再到展示的完整生命周期管理。

### 2. `BookServiceImpl`

作用：实现图书管理业务逻辑。

主要负责：

- 新增图书并校验 ISBN、分类等基础信息
- 修改图书信息
- 删除图书
- 查询图书详情
- 按条件分页查询图书
- 查询新书推荐
- 查询热门图书

业务意义：维护图书主数据，为借阅、收藏、评论、统计等模块提供基础数据支持。

### 3. `BorrowServiceImpl`

作用：实现借阅业务逻辑，是系统中最核心的业务实现类之一。

主要负责：

- 处理借书流程
- 处理还书流程
- 处理续借流程
- 处理借阅记录关联罚款支付
- 查询单条借阅记录
- 分页查询借阅记录
- 查询当前用户个人借阅记录
- 定时检查逾期借阅记录
- 导出借阅记录数据

业务意义：该类连接用户、图书、借阅记录、罚款和预约多个模块，是图书流转业务的核心中枢。

### 4. `CategoryServiceImpl`

作用：实现图书分类管理业务逻辑。

主要负责：

- 创建分类
- 修改分类
- 删除分类
- 查询分类详情
- 查询全部分类
- 构建分类树

业务意义：保证分类结构有序，支持父子级分类关系，方便图书按类别进行组织和展示。

### 5. `ExcelExportServiceImpl`

作用：实现 Excel 导出功能。

主要负责：

- 将借阅记录列表导出为 Excel 文件
- 按响应流返回导出结果

业务意义：支撑后台数据导出需求，方便管理员进行业务统计、归档和线下分析。

### 6. `FavoriteServiceImpl`

作用：实现图书收藏业务逻辑。

主要负责：

- 添加收藏
- 取消收藏
- 查询当前用户收藏列表
- 判断某本书是否已收藏
- 统计某本书被收藏次数

业务意义：为用户提供个性化图书管理能力，同时为热门图书分析提供补充数据。

### 7. `FineServiceImpl`

作用：实现逾期罚款相关业务逻辑。

主要负责：

- 查询当前启用的罚款规则
- 更新罚款规则
- 依据逾期天数生成罚款记录
- 分页查询罚款记录
- 查询个人罚款记录
- 查询罚款详情
- 支付罚款
- 减免罚款
- 统计用户未支付罚款金额
- 统计系统整体罚款数据
- 根据规则计算罚款金额

业务意义：该类将罚款配置、罚款生成、罚款支付与统计分析统一起来，是借阅违规处理的重要模块。

### 8. `NotificationServiceImpl`

作用：实现通知消息业务逻辑。

主要负责：

- 查询当前用户通知列表
- 统计未读通知数量
- 标记单条通知已读
- 全部通知标记已读
- 发送通知消息
- 检查并发送到期提醒
- 检查并发送逾期提醒

业务意义：提升系统主动服务能力，使预约、借阅和逾期等重要状态及时触达用户。

### 9. `ReservationServiceImpl`

作用：实现图书预约业务逻辑。

主要负责：

- 处理图书预约
- 取消预约
- 查询当前用户预约记录
- 查询当前用户有效预约
- 判断用户是否已对某书进行有效预约
- 统计预约队列长度
- 在图书可借时通知下一位预约用户
- 定时处理过期预约
- 在符合条件时完成预约兑现

业务意义：解决图书库存紧张时的排队使用问题，提升图书资源周转效率。

### 10. `ReviewServiceImpl`

作用：实现图书评论业务逻辑。

主要负责：

- 新增评论
- 分页查询某本书评论
- 查询当前用户自己的评论
- 分页查询全部评论
- 删除评论
- 管理员删除评论
- 评论点赞
- 修改评论状态
- 查询最新评论
- 统计图书平均评分
- 统计图书评论数量

业务意义：为图书提供读者反馈机制，同时支持后台进行评论审核与管理。

### 11. `StatisticsServiceImpl`

作用：实现系统统计分析业务逻辑。

主要负责：

- 统计图书总量、用户总量、借阅总量等基础指标
- 生成后台仪表盘展示数据
- 生成高级统计结果
- 统计热门图书、活跃用户、类别借阅、月度借阅等数据

业务意义：帮助管理员从业务数据中了解系统运行情况，并为决策提供依据。

### 12. `UserServiceImpl`

作用：实现用户与认证相关业务逻辑。

主要负责：

- 用户登录
- 用户注册
- 获取当前登录用户信息
- 获取当前登录用户实体
- 分页查询用户
- 查询用户详情
- 更新用户状态
- 审核通过用户
- 驳回用户
- 查询待审核用户
- 删除用户
- 修改密码

业务意义：该类承担用户账号生命周期管理，同时配合登录安全控制，是用户管理模块的核心。

---

## 四、13 个实体类的详细作用

实体类用于描述系统中的业务对象，并与数据库表建立映射关系。

### 1. `BaseEntity`

作用：所有基础业务实体的公共父类。

主要包含：

- 主键 `id`
- 创建时间 `createdAt`
- 更新时间 `updatedAt`

意义：避免各实体重复定义公共字段，提高代码一致性。

### 2. `Announcement`

作用：表示系统公告数据。

主要包含的信息：

- 公告标题
- 公告内容
- 公告类型
- 是否置顶
- 发布状态
- 发布人编号
- 发布人姓名

意义：用于保存平台公告、公示通知等信息。

### 3. `Book`

作用：表示图书基础信息，是图书管理模块的核心实体。

主要包含的信息：

- ISBN
- 图书名称
- 作者
- 出版社
- 出版日期
- 所属分类
- 图书价格
- 馆藏总量
- 可借数量
- 图书简介
- 封面地址
- 馆藏位置
- 状态

意义：承载整个系统中最核心的图书主数据。

### 4. `BorrowRecord`

作用：表示一次借阅行为的完整记录。

主要包含的信息：

- 借阅用户
- 借阅图书
- 借出时间
- 应还时间
- 实际归还时间
- 借阅状态
- 备注
- 续借次数
- 逾期天数
- 罚款金额
- 罚款是否已支付

意义：记录图书流转全过程，是借阅、统计、罚款和通知模块的重要基础数据。

### 5. `Category`

作用：表示图书分类。

主要包含的信息：

- 分类名称
- 分类描述
- 父分类
- 子分类列表
- 排序值
- 该分类下的图书集合

意义：支撑图书分类树结构，便于图书管理与检索。

### 6. `Favorite`

作用：表示用户对图书的收藏关系。

主要包含的信息：

- 收藏用户
- 被收藏图书
- 收藏备注

意义：用于支持用户个人收藏和图书热度统计。

### 7. `FineRecord`

作用：表示一条罚款记录。

主要包含的信息：

- 被处罚用户
- 对应借阅记录
- 罚款金额
- 逾期天数
- 罚款状态
- 支付时间
- 减免时间
- 减免原因
- 操作人编号
- 操作人姓名
- 备注

意义：完整记录罚款产生、支付与减免过程。

### 8. `FineRule`

作用：表示罚款规则配置。

主要包含的信息：

- 每日罚款金额
- 最高罚款金额
- 宽限天数
- 规则描述
- 是否启用

意义：用于统一管理系统罚款计算标准，使罚款逻辑可配置。

### 9. `Notification`

作用：表示系统通知消息。

主要包含的信息：

- 接收用户
- 通知类型
- 通知标题
- 通知内容
- 是否已读
- 关联借阅记录编号

意义：用于保存借阅提醒、逾期提醒、预约通知等消息内容。

### 10. `OperationLog`

作用：表示系统操作日志。

主要包含的信息：

- 模块名称
- 操作类型
- 操作描述
- 请求方法
- 请求地址
- 请求参数
- 返回结果
- 操作人
- 操作人编号
- IP 地址
- 操作时间
- 耗时
- 执行状态
- 错误信息

意义：用于系统审计、运维排查与安全追踪。

### 11. `Reservation`

作用：表示图书预约记录。

主要包含的信息：

- 预约用户
- 预约图书
- 预约状态
- 排队位置
- 通知时间
- 过期时间

意义：用于描述用户预约排队及通知兑现过程。

### 12. `Review`

作用：表示用户对图书发表的评论与评分。

主要包含的信息：

- 评论用户
- 评论图书
- 评分
- 评论内容
- 点赞数
- 评论状态

意义：用于构建图书评价体系和用户互动内容。

### 13. `User`

作用：表示系统用户信息。

主要包含的信息：

- 用户名
- 密码
- 真实姓名
- 邮箱
- 手机号
- 用户角色
- 用户状态

意义：是系统身份管理、权限控制、借阅行为和通知接收的基础实体。

---

## 五、12 个 Repository 的详细作用

Repository 位于数据访问层，负责面向数据库执行查询、统计与持久化操作。

### 1. `AnnouncementRepository`

作用：负责公告数据访问。

主要能力：

- 按条件分页查询公告
- 查询已发布公告
- 查询置顶公告
- 查询最新公告

### 2. `BookRepository`

作用：负责图书数据访问。

主要能力：

- 按 ISBN 查询和校验唯一性
- 锁定图书记录用于并发更新
- 更新可借库存
- 条件分页查询图书
- 高级检索图书
- 查询分类下图书数量
- 查询新书
- 查询热门图书

### 3. `BorrowRecordRepository`

作用：负责借阅记录数据访问。

主要能力：

- 查询带详细关联信息的借阅记录
- 锁定借阅记录
- 条件分页查询借阅记录
- 查询用户个人借阅记录
- 查询某用户当前有效借阅
- 查询逾期记录
- 统计各类借阅数量
- 查询热门图书、活跃用户
- 统计按分类、按月份、按用户角色的借阅数据

### 4. `CategoryRepository`

作用：负责分类数据访问。

主要能力：

- 按名称查询分类
- 判断分类名称是否已存在
- 查询根分类
- 查询某父分类下的子分类
- 统计某分类下图书数量

### 5. `FavoriteRepository`

作用：负责收藏数据访问。

主要能力：

- 查询用户收藏及关联图书
- 查询用户对某本书的收藏关系
- 判断是否已收藏
- 统计用户收藏总数
- 统计某本书被收藏次数

### 6. `FineRecordRepository`

作用：负责罚款记录数据访问。

主要能力：

- 查询用户罚款记录
- 依据借阅记录查询罚款记录
- 分页查询全部罚款记录
- 按状态查询罚款记录
- 统计用户未支付罚款总额
- 统计系统未支付罚款总额
- 按状态统计罚款数量
- 按条件筛选罚款记录

### 7. `FineRuleRepository`

作用：负责罚款规则数据访问。

主要能力：

- 查询当前启用规则
- 查询最新规则

### 8. `NotificationRepository`

作用：负责通知消息数据访问。

主要能力：

- 分页查询用户通知
- 查询用户未读通知
- 统计未读通知数
- 批量标记全部通知为已读
- 判断某类通知是否已发送

### 9. `OperationLogRepository`

作用：负责操作日志数据访问。

主要能力：

- 按条件分页查询日志
- 统计某操作人的日志数量

### 10. `ReservationRepository`

作用：负责预约记录数据访问。

主要能力：

- 查询用户对某本书的有效预约
- 统计某书当前排队人数
- 查询某书排队中的第一位预约
- 查询某书全部等待中的预约
- 查询某用户预约记录
- 查询某用户当前有效预约
- 查询已通知但已过期的预约
- 调整预约队列顺序
- 查询指定用户和图书的已通知预约

### 11. `ReviewRepository`

作用：负责评论数据访问。

主要能力：

- 查询某本书已通过评论
- 查询全部评论
- 查询某用户评论
- 判断用户是否已评论某本书
- 查询用户对某书的评论
- 评论点赞
- 统计图书平均评分
- 统计图书评论数量
- 查询最新评论
- 按关键词搜索评论

### 12. `UserRepository`

作用：负责用户数据访问。

主要能力：

- 按用户名查询用户
- 判断用户名是否已存在
- 按关键字分页查询用户
- 按状态分页查询用户
- 统计指定状态用户数量

---

## 六、3 组单元测试的详细作用

单元测试用于验证关键业务逻辑是否符合预期。

### 1. `BorrowServiceTest`

作用：测试借阅业务实现类中的关键借阅流程。

主要覆盖：

- 还书成功
- 借阅记录不存在时还书失败
- 已归还记录不可重复归还
- 续借成功
- 超过续借次数限制时失败
- 逾期状态下不允许续借
- 查询借阅记录详情成功与失败场景

意义：保证借阅流程中的核心状态转换正确。

### 2. `ReservationServiceTest`

作用：测试预约业务实现类中的关键预约流程。

主要覆盖：

- 预约成功
- 图书不存在时预约失败
- 图书可借时不允许预约
- 已预约同一本书时重复预约失败
- 取消预约成功
- 预约记录不存在时取消失败
- 非本人取消预约时权限校验失败
- 状态不允许时不能取消预约
- 判断是否存在有效预约
- 获取排队长度

意义：保证预约业务的队列规则与权限规则正确执行。

### 3. `UserServiceTest`

作用：测试用户业务实现类中的账号管理与审核逻辑。

主要覆盖：

- 注册成功且初始为待审核状态
- 用户名重复时注册失败
- 审核通过成功
- 非待审核状态下不能重复通过审核
- 驳回用户成功
- 修改密码成功
- 两次密码不一致时修改失败
- 原密码错误时修改失败
- 新旧密码相同时修改失败
- 不能禁用自己
- 不能禁用管理员

意义：保证用户账号生命周期管理和安全规则可靠。

---

## 七、总结

从系统结构上看：

- 控制器负责“接收请求并暴露接口”
- 业务实现类负责“真正执行业务规则”
- 实体类负责“抽象业务对象和数据库表结构”
- Repository 负责“执行数据访问”
- 单元测试负责“验证核心逻辑正确性”

---

## 八、前端实现与界面设计说明

除了后端分层结构外，本系统前端也承担了页面展示、用户交互、路由切换、状态维护和接口调用等任务。

### 1. 前端整体实现方式

本项目的前端主要由 `admin.html`、`user.html` 和公共脚本 `js/common.js` 组成，采用前后端分离的实现思路。前端负责界面展示和交互逻辑，后端负责业务处理和数据存储，二者通过 HTTP 接口进行通信。

前端主要技术实现如下：

- 使用 `Vue 3` 构建页面逻辑和组件式交互
- 使用 `Vue Router` 实现页面路由管理
- 使用 `Pinia` 管理登录状态、用户信息等前端状态
- 使用封装后的 `fetch` 工具统一调用后端接口
- 使用 `localStorage` 保存登录令牌和用户信息
- 使用 `Element Plus` 构建表单、表格、弹窗、菜单、消息提示等功能界面
- 使用 `Tailwind CSS` 完成页面布局、颜色、间距、圆角、阴影等样式设计

其中：

- `admin.html` 主要实现后台管理端界面
- `user.html` 主要实现读者端界面
- `js/common.js` 主要封装公共配置、HTTP 请求方法、图片异常处理和时间格式化等通用逻辑

### 2. 前端页面是如何运行的

前端运行流程可以概括为：

1. 用户打开页面后，由 Vue 创建前端应用实例。
2. Vue Router 根据当前地址加载对应页面视图。
3. 页面中的按钮、表单、表格、弹窗等组件由 Element Plus 和 Vue 共同驱动。
4. 当前登录状态、用户信息等内容由 Pinia 和 `localStorage` 维护。
5. 当前端需要读取或提交数据时，通过 `js/common.js` 中封装的 HTTP 请求方法调用后端接口。
6. 后端返回结果后，前端再将数据渲染到列表、详情页、统计卡片或消息提示中。

这种方式使前端专注于交互与展示，后端专注于业务处理，整体结构较清晰。

### 3. UI 界面主要是怎么写的

本系统的 UI 采用“组件库 + 工具类样式 + 自定义样式”的方式完成。

具体来说：

- `Element Plus` 负责提供现成的交互组件，例如按钮、输入框、对话框、表格、分页器、菜单、消息提示等
- `Tailwind CSS` 负责快速定义页面布局和视觉样式，例如弹性布局、边距、字号、颜色、圆角、阴影、宽高等
- 页面中还补充了少量自定义 CSS，用于实现卡片悬停、渐变背景、动画过渡、导航栏玻璃效果等视觉增强

例如在用户端页面中，可以看到很多类似下面的设计方式：

- 用 `flex`、`items-center`、`justify-between` 控制横向布局
- 用 `p-4`、`mt-6`、`gap-3` 控制内外边距
- 用 `rounded-2xl`、`shadow-lg`、`bg-white` 控制卡片外观
- 用 `text-gray-500`、`font-bold`、`text-sm` 控制文本样式

因此，UI 并不是全部手写传统 CSS，而是通过现成组件和样式类组合出来的，这样开发效率更高，界面风格也更统一。

### 4. `Tailwind CSS` 是什么

`Tailwind CSS` 是一种实用优先的 CSS 框架。它不直接提供完整页面模板，而是提供大量可组合的样式类，开发者可以把这些类直接写在 HTML 标签的 `class` 属性中，从而快速完成页面样式设计。

它常见的能力包括：

- 控制布局方式，如 `flex`、`grid`
- 控制边距和内边距，如 `mt-4`、`px-6`
- 控制颜色，如 `bg-white`、`text-gray-700`
- 控制圆角和阴影，如 `rounded-xl`、`shadow-md`
- 控制宽高，如 `w-full`、`h-10`
- 控制响应式布局，如 `md:grid-cols-2`

相比传统方式中需要单独写大量 CSS 选择器，Tailwind CSS 可以直接在标签上完成样式组合，因此更适合快速构建现代化界面。

### 5. `Tailwind CSS` 示例说明

下面是一个简单示例：

```html
<div class="bg-white rounded-2xl shadow-lg p-6 flex items-center justify-between">
  <div>
    <h3 class="text-lg font-bold text-gray-800">图书借阅统计</h3>
    <p class="text-sm text-gray-500 mt-2">显示当前系统中的借阅数量变化情况</p>
  </div>
  <button class="bg-indigo-600 text-white px-4 py-2 rounded-lg hover:bg-indigo-700">
    查看详情
  </button>
</div>
```

这个示例中各个类的含义如下：

- `bg-white`：设置白色背景
- `rounded-2xl`：设置较大的圆角
- `shadow-lg`：设置较明显的阴影
- `p-6`：设置内边距
- `flex items-center justify-between`：让内容使用弹性布局，并在垂直方向居中、左右两端分布
- `text-lg font-bold text-gray-800`：设置标题字号、加粗和颜色
- `text-sm text-gray-500 mt-2`：设置说明文字样式和上边距
- `bg-indigo-600 text-white px-4 py-2 rounded-lg`：设置按钮背景色、文字颜色、内边距和圆角
- `hover:bg-indigo-700`：设置鼠标悬停时按钮颜色变化

也就是说，使用 Tailwind CSS 时，开发者通常不需要先写一段单独的 `.card { ... }` 或 `.button { ... }` 样式，而是可以直接通过类名把页面外观组合出来。

### 6. 前端实现的整体意义

本系统前端采用 Vue 3、Vue Router、Pinia、Element Plus 和 Tailwind CSS 进行实现，具有以下优点：

- 页面结构清晰，功能划分明确
- 用户端与管理端职责分离
- 组件化程度较高，便于维护
- 页面样式开发效率高
- 与后端接口衔接清晰，便于扩展

因此，前端不仅实现了系统的可视化操作界面，也保证了系统在交互体验、功能组织和后续维护方面具备较好的可用性。

如果你后面需要，我可以继续把这份文档再加工成两种版本：

- 论文版：语气更正式、更像毕业设计正文
- 答辩版：每类压缩成适合 PPT 直接展示的短句
